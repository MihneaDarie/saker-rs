[package]
name = "saker-rs"
version = "0.1.2"
edition = "2021"
description = "Fast SIMD-accelerated linear algebra"
license = "MIT"

[dependencies]
rayon = "1.11.0"

[target.'cfg(all(target_arch = "aarch64", target_os = "macos"))'.build-dependencies]
libc = "0.2"